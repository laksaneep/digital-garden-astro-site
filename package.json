{
  "name": "digital-garden-astro-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync-notes.mjs",
    "dev": "npm run sync && astro dev",
    "build": "npm run clean && npm run sync && astro build && pagefind --site dist",
    "preview": "astro preview",
    "clean": "rm -rf dist .astro node_modules/.astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^5.13.0",
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.6.0",
    "tailwindcss": "^4.3.3",
    "unist-util-visit": "^5.1.0"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
